import React from 'react'
import { Segment, SegmentProps } from 'semantic-ui-react'

import './segment.less'

export const HarvestSegment = (props: SegmentProps) => (<Segment {...props} className='harvest' />)

export default HarvestSegment
